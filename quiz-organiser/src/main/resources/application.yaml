spring:
  datasource:
    url: jdbc:mysql://localhost:3306/quizdb?allowPublicKeyRetrieval=true&serverTimezone=UTC&useSSL=false
    username: root
    password: Mother@95
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10

  jpa:
    hibernate:
      ddl-auto: update
    # properties:
    #   hibernate:
    #     dialect: org.hibernate.dialect.MySQL8Dialect


server:
  port: 8080